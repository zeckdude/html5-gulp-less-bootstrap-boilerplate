/* ==========================================================================
 Add all JS here
 ========================================================================== */

$( document ).ready(function() {
  // Add all JS here that needs to be run after all the html has been loaded.
  // It executes when the DOM is ready, e.g. all elements are there to be found/used, but not necessarily all content (see window.load function below).
});

$( window ).resize(function() {
  // Add all JS here that needs to be run whenever the window is resized.
});

$( window ).load(function() {
  // Add all JS here that needs to be run after all content has been loaded.
  // It executes later (or at the same time in the worst/failing cases) when images and such (objects, iframes) are loaded, so if you're using image dimensions for example, you often want to use this instead.
});